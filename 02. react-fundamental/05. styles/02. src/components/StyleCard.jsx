const StyleCard = () => {
  const styles = {
    backgroundColor: "lightblue",
    padding: "20px",
    borderRadius: "10px",
    textColor: "darkblue",
  };
  return (
    <div style={styles}>
      <h1>StyleCard</h1>
      <p>This is a card with styles</p>
    </div>
  );
};

export default StyleCard;
